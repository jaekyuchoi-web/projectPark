# Vibe Coding Prompt — HLB글로벌 특관자 명세서 생성 웹앱 (v2)

아래 사양대로 **HLB글로벌(주) 분기 특수관계자(특관자) 명세서 생성 웹앱**을 만들어줘. 회계 담당자가 5종의 엑셀을 올리면, 서버가 자동으로 당기 특관자 명세서·지배구조·오류목록 3개 파일을 만들어 **다운로드만** 제공하는 도구다.

---

## 0. 절대 준수 요구사항 (★ 반드시 모두 구현)

**a) 파일 형식·입출력 검증 (최우선)**
- 입력은 **모든 엑셀 형식 지원**: `.xlsx`, `.xlsm`, `.xls`, `.xlsb`, `.csv`. (openpyxl + xlrd + pyxlsb + pandas 등으로 분기 처리)
- **입출력 오류를 선행적으로 철저히 검증**한다: 업로드 즉시 (1) 확장자·MIME·매직바이트 확인, (2) 워크북 열기 성공 여부, (3) 시트/헤더 존재 여부, (4) 손상·암호화·0바이트 파일 차단. 실패는 즉시 사용자에게 사유와 함께 알린다(처리 진행 금지).
- **출력 파일은 생성 후 반드시 다시 열어 검증**: openpyxl로 재오픈 + LibreOffice headless 재계산(`soffice --headless --calc --convert-to xlsx`) 후 전 셀을 스캔해 `#REF! #VALUE! #DIV/0! #N/A #NAME?` **0건**임을 확인. 오류가 있으면 다운로드 제공 전 실패 처리하고 로그를 오류목록에 남긴다. (사용자가 파일을 열 때 오류가 절대 나지 않도록 보장)

**b) 입력 내용 비표시 (파일명 리스트만)**
- 업로드된 입력 파일은 **파일명만 리스트 형태로 표시**한다. 셀 값·시트·미리보기 등 **내용은 절대 화면에 렌더링하지 않는다.** (5개 슬롯 각각에 "파일명 + 인식된 종류 + 검증 통과/실패 뱃지"만)

**c) 사업자명(거래처명) AI 정규화**
- 약어·띄어쓰기·지점 표기·구사명 등으로 **동일 업체의 '사업자명'이 제각각**이다. 이를 하나로 정리하기 위해 **AI agent(ChatGPT API)를 호출**한다.
- 동작: 입력들에서 distinct 사업자명을 추출 → "특관자 상호 정리" 파일(있으면 시드/기준으로 함께 전달) → ChatGPT API에 "각 표기를 대표(정규) 법인명으로 매핑하라"고 요청 → 매핑 결과(JSON)로 통합. 비용·레이트리밋·실패에 대비해 배치 호출·캐시·재시도·휴리스틱 폴백을 둔다.
- **ChatGPT API 키는 `secret.env`에 입력하도록 안내**한다. 리포지토리에 `secret.env.example`(예: `OPENAI_API_KEY=` , `OPENAI_MODEL=gpt-4.1-mini`)을 두고, `secret.env`는 `.gitignore`에 추가, 서버 기동 시 로드. 앱 첫 화면/설정에 "secret.env에 OPENAI_API_KEY를 넣어주세요" 안내와 키 누락 시 명확한 에러를 표시. (키 값 자체는 화면·로그에 노출 금지)

**d) 출력물 3종**
1. **당기 특관자 명세서**(.xlsx)
2. **지배구조**(.xlsx)
3. **오류 목록**(.xlsx 또는 .csv)

**e) 입력 파일 5종 (당기 명세서 정답 파일은 없음)**
1. 전기 이월 소스 — 전기말 계정별 잔액 명세서
2. 당기 거래 내역 소스 — 당기 계정별 원장(분개 라인)
3. 당기 채권채무 잔액 검증용 소스 — 당기말 계정별 잔액 명세서(= 검증 기준)
4. 특관자 상호 정리 — 표기 차이 정리(정규화 기준/시드)
5. 전기 특관자 명세서 — 양식·수식·전기 비교열의 기준
- 파일명이 제각각일 수 있으므로 **내용 기반 자동 분류** + 사용자가 슬롯을 수동 지정/교정할 수 있는 UI 둘 다 제공. (당기 명세서에 대한 정답 파일은 제공되지 않으며, 검증은 3번 소스로 자체 수행)

**f) 출력 내용 비표시**
- **어떠한 경우에도 출력물의 내용(셀 값·표·차트·미리보기 등)을 앱 페이지에 표시하지 않는다.** 처리 완료 후 제공하는 것은 **다운로드 버튼/링크뿐**이다. 서버 로그·에러 메시지에도 산출 수치를 출력하지 않는다.

---

## 1. 기술 스택(권장)
- 백엔드: Python(FastAPI) + openpyxl/pandas/xlrd/pyxlsb, LibreOffice(headless, 재계산용), OpenAI SDK.
- 프런트: 간단한 단일 페이지(React 또는 서버사이드 템플릿). 상태: 업로드 → 검증 → 처리 → 다운로드.
- 파일은 메모리/임시 디렉터리에서 처리하고 세션 종료 시 정리. 비밀값은 `secret.env`.

## 2. 화면 흐름(UX)
1. **설정 안내**: secret.env에 OPENAI_API_KEY 입력 여부 점검·안내.
2. **업로드**: 5개 슬롯(드래그&드롭). 업로드 즉시 (a) 검증 실행 → 슬롯별 **파일명 + 인식 종류 + 검증 뱃지**만 표시(내용 비표시).
3. **분류 확인**: 자동 분류 결과를 사용자가 슬롯 매핑으로 교정 가능.
4. **실행**: "명세서 생성" 클릭 → 진행 상태(단계별 스피너/로그 요약, 단 수치·내용 비노출).
5. **결과**: 3개 출력 파일 **다운로드 버튼만** 노출. 내용 미리보기 없음. 오류·검토 사항 건수만 요약(상세는 오류목록 파일로 다운로드).

## 3. 처리 파이프라인(서버)
1. **검증/분류**: a)·e) 수행. 실패 즉시 중단·보고.
2. **사업자명 정규화**: c) — ChatGPT로 표기→정규명 매핑(특관자상호정리 시드 결합). "특관자 상호 정리"의 들여쓰기(indent: 0=정규명, 1=이표기) 구조도 1차 매핑으로 활용하고, 미매칭만 AI로 해소.
3. **특관자 판정**: 정규명 집합에 속하는 거래처만 특관자.
4. **38.x 집계**(도메인 로직, §4):
   - 38.1 거래내역(원장), 38.2 채권채무 잔액(검증용 잔액명세서), 38.3 대손충당금, 38.4 자금거래(당기 발생분), 38.5 경영진보상(인사자료 없으면 공란).
5. **명세서 생성**: "전기 특관자 명세서"를 템플릿으로 39.2 피벗을 채우고(특관자 시트는 SUMIF로 당기 자동계산), 전기 비교열 이월.
6. **자체 검증**: (전기이월+당기거래) 재구성 잔액 ↔ 검증용(3번) 대조. 불일치는 검증용에 맞추고 오류목록에 기록.
7. **출력 안전성**: openpyxl 저장 → LibreOffice 재계산 → 수식오류 0 확인(a). 실패 시 다운로드 차단.
8. **지배구조·오류목록 생성** → 다운로드 제공(f: 내용 비표시).

## 4. 도메인 로직 (특관자 명세서 산출 규칙)
> 대상: **HLB글로벌(주)** (사업자명 "에이치엘비글로벌㈜"). **HLB(에이치엘비)·HLB생명과학 등은 별개 회사**.
>
> **"전기"의 정의 = 직전 결산년도**(전년도 연간 결산 = 전년도 4분기). 결산은 연 1회(연말)뿐이므로 **당기가 어느 분기든 전기는 항상 직전년도 4분기(연간 결산)로 동일**하다.
> - 예: 당기가 **2026년 1·2·3·4분기**이면 → 전기는 **모두 2025년 4분기(연간 결산)**.
> - 따라서 모든 분기의 전기이월·전기말·대손충당금/미수수익 증감·자금대여 기준선은 단일(= 직전년도 4분기)이며 분기별로 바뀌지 않는다. 당기 거래(38.1/38.4)는 회계연도 누적(YTD) 기준이다.
> - **"전분기"·"전반기"는 전기가 아니다.** 명세서 양식에 별도 참고 블록(예: 38.4 전반기)으로 나타날 수 있으나 비교기준인 "전기"와 혼동하지 말 것.

**38.1 거래내역(당기, 원장)**
- 매출 = 제품·상품·용역·미디어·렌탈료 매출(대변−차변)
- 매출등 기타 = **이자수익만**(원장 이자수익 + **미수수익 잔액증감 역산**: 당기말−전기이월−원장 미수수익 순증)
- 매입 = 원재료(차변−대변)
- 매입등 기타 = 기타비용(접대·복리후생·여비교통·운반·지급임차료·판매수수료·지급수수료 **순액**) + 자산취득(건설중인자산·상표권·시설장치)
- **지급임차료의 전대(sublease) 음수 라인은 제외(총액 처리)**

**38.2 채권채무 잔액(당기말, 검증용 잔액명세서, 총액=대손 차감 전)**
- 컬럼: 매출채권 · 대여금 · 기타채권 · **투자전환사채** · 기타채무 · **발행전환사채**
- 매출채권 = 외상매출금 + 받을어음(**장기외상매출금 제외**)
- 대여금 = 단기+장기대여금 / 기타채권 = 미수금+미수수익+선급금 / 기타채무 = 미지급금+선수금+예수금 / 매입채무 = 외상매입금
- 투자전환사채·발행전환사채는 별도 항목(사채 회차별 보유자 확인 필요)

**38.3 대손충당금**: 당기말 = 잔액명세서 대손충당금 합. 당기 대손상각비 = 당기말−전기말(증가분, 0이면 공란).
**38.4 자금거래**: **당기 발생분만**(전기/전반기 이월 블록 제외). 자금대여 = 원장 대여금 차변 증가(전기이월 라인 제외).
**38.5 주요 경영진 보상**: 인사·급여(임원 식별·배분) 자료 없으면 표 유지 + **숫자 공란**.

**분리 항목(상호 상계/합산 금지)**: 미지급금/선수금, 미수금/미수수익, 매입채무/매입선급금, 매출채권/선수수익 — 계정별 산정 후 버킷 합산만.

**개별표시 vs "기타"**: 전기 명세서의 행 구성을 따른다. 개별 표시 대상이 아닌 특관자는 "기타" 행으로 합산.

## 5. 출력물 사양
- **당기 특관자 명세서**: 특관자/39.1/39.2 시트. '특관자' 시트가 사업보고서 게재용. 전기 양식·수식 유지, 교차검증 셀 전부 참, 수식오류 0.
- **지배구조**: 정규 법인명별 [구분(종속기업/관계기업/기타특수관계자(상장·비상장)/주요경영진), HLB글로벌과의 관계, 분개장 이표기(별칭), 비고]. 깔끔한 표 서식.
- **오류 목록**: [구분, 대상(파일/시트/거래처/계정), 내용, 권고조치]. 항목 예 — 정규화 미매칭 거래처, 검증용 대비 불일치 셀, 자동 산출 불가(경영진 보상·전환사채 보유자), 파일/형식 경고.

## 6. 비기능 요구
- 견고한 예외 처리(어느 단계 실패든 사용자에게 명확히, 산출 수치는 비노출).
- 큰 원장(수만 행) 처리 성능 고려(스트리밍/청크).
- 재현성: 동일 입력 → 동일 출력. 처리 로그(수치 제외)와 버전 기록.
- 보안: secret.env 비노출, 업로드 파일 세션격리·만료 삭제, 출력 내용 화면/로그 비표시(f).

---
### 참고 의사코드 (정규화 핵심)
```python
# 1) 특관자상호정리(들여쓰기) 1차 매핑
norm = {}; cur=None
for cell in name_pivot_colA:               # openpyxl, data_only=False
    ind = cell.alignment.indent or 0
    if ind == 0: cur = cell.value; norm[cur]=cur
    elif cur:    norm[cell.value]=cur
# 2) 미매칭 사업자명만 ChatGPT로 정규명 매핑
unmatched = [n for n in distinct_names if n not in norm]
mapping = openai_map_to_canonical(unmatched, seed=norm)  # secret.env의 OPENAI_API_KEY 사용
norm.update(mapping)
# 3) 정규명 집합에 속하면 특관자
is_affiliate = lambda name: norm.get(name) in set(canonical_names)
```
